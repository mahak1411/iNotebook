// Context basically hold all the state that is related to notes
import { createContext } from "react";

// Just created a new context
const NoteContext = createContext();


export default NoteContext;